# 更新日志

本文件记录项目的所有重要变更。

## [0.1.7] - 2026-08-24

### 修复

- `code-gen-tdd --feature-id` 的自动提交范围现在严格限制为 `feature-map.json` 映射到的服务，避免提交无关服务的改动。
- 活动中的 TDD 运行禁止切换到另一个功能点，避免执行状态、生成代码和自动提交使用不同的功能点标识。
- 多服务流程进入修复阶段时只重新处理失败或阻塞的服务；旧版执行状态仍兼容全量修复。
- 在代码生成前校验功能点编号、服务映射、主服务归属和验收标准，错误的 `feature-map.json` 会提前阻断流程。
- 服务仓库必须存在且为目录，并通过规范路径和真实路径双重边界校验，阻止相对路径、Windows 大小写及符号链接越界。
- 协作服务执行子任务时使用各自需求目录内的 `feature-map.json`，确保按服务、按功能点写入正确仓库。

### 变更

- 功能点范围约束改为代理提示词中的显式硬约束，覆盖测试设计、代码实现、代码审查、测试生成和测试执行阶段。
- Quick Start 与架构文档补充按功能点执行及自动提交范围说明。
- 清理历史 ESLint 警告，当前代码检查为零警告。

### 验证

- 通过 220 个单元测试、12 个契约测试和 13 个集成测试，并通过类型检查、代码检查和包结构校验。

## [0.1.6] - 2026-08-24

### 变更

- 发布并标记 `0.1.5` 引入的多服务、功能点映射与文档更新。

## [0.1.5] - 2026-08-24

### 新增

- 多服务 `code-gen-tdd` 会根据 `apps.json` 逐个服务执行 TDD 阶段，并在各自仓库生成产物；`--auto-comit` 会逐仓库提交、推送并返回服务明细。
- `implementation-plan` 会将 `prd.md`、`tech-design.md` 和功能点映射同步到协作服务需求目录。
- 技术方案新增“功能点 × 服务”矩阵和 `feature-map.json` 契约；`code-gen-tdd --feature-id F-001` 只实现映射到的服务，产物隔离在 `ai/F-001/`。

### 变更

- 重写 Quick Start、架构与开发说明，明确 Git 默认安装、多服务绝对路径定位和恢复调用方式。

## [0.1.4] - 2026-08-24

### 新增

- `implementation-plan`、`code-gen-tdd` 和 `bugfix` 新增 `--auto-comit` 参数；成功完成后自动执行 `git add --all`、提交并推送，包含新增和删除的文件。
- 兼容标准拼写 `--auto-commit`；参数会跨确认门和恢复流程持久化。

## [0.1.2] - 2026-08-24

### 新增

- `mrd-to-code` 支持通过 `rawUserRequest` 直接输入需求，不再强制要求 MRDoc URL。
- 直接输入需求时，完整流程复用 `implementation-plan` 的本地来源落盘、服务路由、澄清、代码生成与归档能力。

## [0.1.1] - 2026-08-24

### 新增

- `implementation-plan` 支持通过 `rawUserRequest` 直接输入需求，不再强制要求 MRDoc URL。
- 直接输入的需求会保存为带来源校验和的 `mrd-original.md`，再进入既有的服务路由、澄清、PRD 与技术方案流程。
- 新增直接输入需求的命令示例和自动化测试覆盖。

## [0.1.0]

### 新增

- DSH 特性开发工作流 Bundle 首次发布。

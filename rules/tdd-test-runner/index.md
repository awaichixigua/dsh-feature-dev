# tdd-test-runner 规则索引

规则根目录由阶段提示提供，以下路径均相对于该目录。

执行测试与编写报告前必须读取：

1. `library/testing/phase-result-contract.md`
2. `library/testing/coverage.md`
3. `library/testing/validation.md`

报告必须基于实际命令输出；不能执行测试时返回 `block` 并说明环境、依赖或命令原因，不能把推测结果写成通过。

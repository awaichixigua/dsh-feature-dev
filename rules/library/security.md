# 安全规范

适用于用户输入、身份权限、文件上传、SQL、密钥、日志和外部调用。

- 所有外部输入按接口边界校验；文件上传校验类型、大小、名称和目标路径，防止路径遍历。
- SQL 必须参数绑定，禁止字符串拼接；查询只取需要字段。
- 禁止在源码、测试夹具、日志或报告中写入密钥、密码、令牌或真实敏感数据。
- 日志不得泄露身份证号、手机号、银行卡号等敏感字段；确需记录时按项目规则脱敏。
- 外部调用配置连接和读取超时，并对失败提供可追踪的错误上下文。
- 发现疑似凭据泄露或高风险安全缺陷时，停止扩展改动、在结果中明确标记，并建议轮换或人工处置。

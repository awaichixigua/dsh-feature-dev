# 测试环境与数据规则

- 测试默认采用 mock-first：数据库、Redis、消息、远程服务和时间依赖均使用 mock、stub 或可控替身。
- 测试数据在用例内构造或通过清晰的 fixture 工厂构造，避免读取开发机环境、共享数据或真实账号。
- Spring MVC 测试优先使用 standalone MockMvc；仅在测试目标确实依赖 Spring 装配时启动最小上下文。
- OpenFeign 或其他 HTTP 客户端只验证调用契约、序列化和失败分支，不发起真实网络请求。
- 无法隔离的环境依赖必须在报告中说明，并将执行结果标记为阻塞或受限，不能伪造覆盖率结论。

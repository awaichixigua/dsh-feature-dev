# 测试生成与执行校验

生成测试前确认：目标功能点、测试规格、被测入口、外部依赖和预期断言均可定位。

生成后确认：

- 测试文件位于项目既有测试目录，命名与框架约定一致。
- Mock、fixture、断言和异常场景与 `test_spec.md` 对应。
- 没有真实外部调用、`Thread.sleep`、随机不稳定断言或无关生产代码改动。
- 失败结果包含可定位的命令、测试名或错误位置，供修复阶段判断 `test_defect` 或 `production_defect`。

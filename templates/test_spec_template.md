# {需求名称} 测试规格

> 本文档仅保留 P0 所需结构，目标是为 `testcode-gen` 提供低噪声、可解析输入。
> 测试实现口径固定为 **Java/JUnit**。禁止在本文档中出现 `Spock`、`Groovy`、`Spec.groovy`，也禁止写“Spock 或 JUnit”这类二选一表述。

---

## 1. 需求分析

### 1.1 业务背景

### 1.2 功能点拆解

| 功能点 | 描述 | 优先级 |
|--------|------|--------|
| | | P0 |

### 1.3 用户故事

### 1.4 边界条件与约束

---

## 2. 需求实现分析

### 2.1 技术方案概述

### 2.2 涉及模块/服务

| 模块 | 服务名 | 职责 |
|------|--------|------|
| | | |

### 2.3 被测入口

| 入口类型 | 类/接口 | 方法/主题 | 说明 |
|----------|---------|-----------|------|
| HTTP/OpenFeign/MQ | | | |

### 2.4 外部依赖

| 依赖服务 | 用途 | 调用方式 |
|----------|------|----------|
| | | HTTP/OpenFeign/MQ |

### 2.5 配置与数据影响

| 类型 | 名称 | 说明 |
|------|------|------|
| 配置/表/状态 | | |

---

## 3. 需求测试分析

### 3.1 测试策略

| 项 | 说明 |
|----|------|
| 入口类型 | |
| 主测试方式 | 组件测试 |
| 增量覆盖目标 | **精确增量行覆盖率 ≥80%**（先在规格补全用例意图，再生成/运行测试） |

### 3.2 测试范围

**包含：**

- 

**不包含：**

- 

### 3.3 风险点识别

| 风险点 | 影响 | 缓解措施 |
|--------|------|----------|
| | 高/中/低 | |

### 3.4 覆盖率意图清单

> 必须填写，确保变更方法全覆盖。对每个变更方法标注测试入口类型（公共入口/反射调用）及对应用例 ID。

| 变更方法（类.方法） | 访问修饰符 | 测试入口类型 | 对应用例 ID | 覆盖意图 |
|---------------------|-----------|-------------|-------------|----------|
|                     |           |             |             |          |

---

## 4. 测试用例

> 根据技术方案逐一判断 12 类场景是否适用；适用场景必须生成测试用例，不适用场景保留标题并写 `N/A：本需求暂无此类场景`，禁止为了凑齐 12 类场景虚构用例。

### 4.1 正向场景

| 用例ID | 场景描述 | 前置条件 | 输入 | 预期结果 |
|--------|----------|----------|------|----------|
| TC001 | | M1-[配置]： | | EX1 - ；EX2 - |

### 4.2 异常场景

| 用例ID | 场景描述 | 前置条件 | 输入 | 预期结果 |
|--------|----------|----------|------|----------|
| TC101 | | M1-[MOCK]： | | EX1 - ；EX2 - |

### 4.3 边界场景

| 用例ID | 场景描述 | 前置条件 | 输入 | 预期结果 |
|--------|----------|----------|------|----------|
| TC201 | | - | | EX1 - ；EX2 - |

### 4.4 并发场景

### 4.5 幂等场景

### 4.6 状态流转场景

### 4.7 数据一致性场景

### 4.8 安全场景

### 4.9 性能场景

### 4.10 兼容性场景

### 4.11 配置场景

### 4.12 补偿/回滚场景

> 不适用的场景章节填写：`N/A：本需求暂无此类场景`。

---

## 5. 测试数据准备

### 5.1 基础数据依赖

### 5.2 动态数据构造

| 数据对象 | 构造方式 | 说明 |
|----------|----------|------|
| | Mock 构造 | |

### 5.3 Mock 与配置清单

| 类型 | 编号 | 内容 |
|------|------|------|
| MOCK/配置 | M1 | |

---

## 6. 测试环境

| 项 | 要求 |
|----|------|
| 测试框架 | `Java/JUnit5 + Mockito` |
| HTTP 测试 | standalone `MockMvc`（`MockMvcBuilders.standaloneSetup(controller)`） |
| 非 HTTP 测试 | `@ExtendWith(MockitoExtension.class)` |
| 外部依赖 | 全部 `Mockito.mock()` / `@Mock` |
| 是否启动本地服务 / 容器 | `false` |
| 数据库 | Mock / Stub |
| Redis | Mock / Stub |
| 事务 | `N/A` |

---

## 7. AI-SDD 测试执行

1. `testcode-gen` 基于本规格生成 Java/JUnit 测试代码与 `test_file_list`。
2. `tdd-test-runner` 按清单运行测试并生成覆盖率报告。
3. 若覆盖率未达 §3.1 目标，优先补充本规格后再生成并运行测试。

---

## 8. 附录

### 8.1 术语

### 8.2 备注
